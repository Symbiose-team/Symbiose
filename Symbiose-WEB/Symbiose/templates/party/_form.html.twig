{% extends 'base.html.twig' %}
{% block back %}
<div class="row mb-4">
    <div class="col">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}